absoulute directories in makefiels are wrong

test setting lintian, autopkgtest, most come from outside, should need to regenerate makefile if it's false or not

merge verify into pkg-builder

delete unneded code 

we might want to hash makefile and store the hash in verify , not sure if this makes sense