# Auto-generated by pkg-builder — do not edit
# Runtime: node | Distribution: bookworm

$distribution = 'bookworm';
$build_arch_all = 1;
$build_source = 1;
$source_only_changes = 1;
$chroot_mode = 'unshare';
$run_piuparts = 0;
$apt_upgrade = 0;
$apt_distupgrade = 0;
$run_autopkgtest = 0;
$verbose = 1;
$run_lintian = 0;
$build_dir = "$ENV{HOME}/.pkg-builder/packages/bookworm/hello-world-typescript-1.0.0-1";

# Runtime: Node.js
my $binary_url = 'https://nodejs.org/download/release/v20.12.2/node-v20.12.2-linux-x64.tar.gz';
my $binary_checksum = 'f8f9b6877778ed2d5f920a5bd853f0f8a8be1c42f6d448c763a95625cbbb4b0d';

my @runtime_commands = (
    'apt install -y wget',
    "wget -q -O /tmp/node.tar.gz $binary_url",
    "echo \"$binary_checksum  /tmp/node.tar.gz\" > /tmp/hash_file.txt && sha256sum -c /tmp/hash_file.txt",
    'rm -rf /usr/share/node && mkdir -p /usr/share/node && tar -C /usr/share/node -xzf /tmp/node.tar.gz --strip-components=1 && rm -f /tmp/node.tar.gz /tmp/hash_file.txt',
    'ln -s /usr/share/node/bin/node /usr/bin/node',
    'ln -s /usr/share/node/bin/npm /usr/bin/npm',
    'ln -s /usr/share/node/bin/npx /usr/bin/npx',
    'ln -s /usr/share/node/bin/corepack /usr/bin/corepack',
    'apt remove -y wget',
    'node --version',
    'npm --version',
);

$external_commands = {
    'chroot-setup-commands' => [
        @runtime_commands,
    ],
};

1;
