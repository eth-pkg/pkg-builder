#!/usr/bin/env sh

exec 2>&1

set -e

test_binary_in_path(){
  ls -al /usr/lib/hello-world-dotnet/bin
  assertEquals "/usr/bin/hello-world" "$output"
}

test_invocation_with_dotnet(){
  output="$(hello-world)"
  assertContains "Hello, World!" "$output"
}

. shunit2