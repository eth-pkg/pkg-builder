# Auto-generated by pkg-builder — do not edit
# Runtime: go | Distribution: noble

$distribution = 'noble';
$build_arch_all = 1;
$build_source = 1;
$source_only_changes = 1;
$chroot_mode = 'unshare';
$run_piuparts = 0;
$apt_upgrade = 0;
$apt_distupgrade = 0;
$run_autopkgtest = 0;
$verbose = 1;
$run_lintian = 0;
$build_dir = "$ENV{HOME}/.pkg-builder/packages/noble/hello-world-go-1.0.0-1";

# Runtime: Go
my $binary_url = 'https://go.dev/dl/go1.22.2.linux-amd64.tar.gz';
my $binary_checksum = '5901c52b7a78002aeff14a21f93e0f064f74ce1360fce51c6ee68cd471216a17';

my @runtime_commands = (
    'apt install -y wget',
    "wget -q -O /tmp/go.tar.gz $binary_url",
    "echo \"$binary_checksum  /tmp/go.tar.gz\" > /tmp/hash_file.txt && sha256sum -c /tmp/hash_file.txt",
    'rm -rf /usr/local/go && tar -C /usr/local -xzf /tmp/go.tar.gz && rm -f /tmp/go.tar.gz /tmp/hash_file.txt',
    'ln -s /usr/local/go/bin/go /usr/bin/go',
    'chmod -R a+rwx /usr/local/go/pkg',
    'apt remove -y wget',
    'go version',
);

$external_commands = {
    'chroot-setup-commands' => [
        'apt install -y software-properties-common',
        'add-apt-repository universe',
        'add-apt-repository restricted',
        'add-apt-repository multiverse',
        'apt update',
        @runtime_commands,
    ],
};

1;
