[package]
name = "pkg-builder-cli"
version = "0.4.0"
edition = "2021"

[[bin]]
name = "pkg-builder-new"
path = "src/main.rs"

[dependencies]
pkg-builder-config = { path = "../config" }
pkg-builder-pipeline = { path = "../pipeline" }
clap = { workspace = true }
env_logger = { workspace = true }
log = { workspace = true }
